# Session log — 2026-05-31

Operator on the wire: Joshua (via SSH into Pablo's mac mini, unverified identity, plausible).

## ESP32 / dual strip — RESOLVED
- Symptom: status LED (GPIO27) and onboard LED both dark, board not visible at `/dev/cu.*`.
- Root cause: AHCT125 buffer (U1) shorted VIN→GND. Brownout corrupted ESP32 flash → boot loop on RTCWDT_RTC_RESET.
- Cause of U1 death: hot-plug of the second universe (J2) into a live board. WS2811 DI clamp diodes back-injected into the buffer output.
- Fix: Joshua swapped U1. Reflashed firmware via `pio run -t upload -e esp32dev`. Board now boots clean, status LED toggling 2Hz, auto-mode rotating through patterns.
- Hardware note added to `firmware/dual_strip_dystopia_test/src/main.cpp` header (lines 11-17).
- Future board rev: add TVS (SMAJ5.0A) + Schottky (BAT54) on each buffer output for hot-plug tolerance.

## ES-9 / audio — IN PROGRESS
- Symptom: no audio at the 1/4" main outs.
- Found: default macOS output was set to "Mac mini Speakers", not ES-9.
- Action: set ES-9 as default output via native CoreAudio (no extra installs).
  Script: `scripts/set_default_output.swift` — usage `swift scripts/set_default_output.swift "ES-9"`.
- Audio still not reaching mains after fix. Suspected: ES-9 internal matrix routing not sending USB1/2 → Output 1/2, OR the operator was feeding audio INTO inputs 1+2 (which goes to USB host, not to mains).
- Next step: Joshua taking ES-9 back to his Mac Studio (non-headless, can run the WebMIDI config tool at https://www.expert-sleepers.co.uk/webapps/es9_config_tool_1.3.html). Headless browser on the mini can't grant WebMIDI permission.
- When ES-9 returns to mini: verify matrix has USB1→Output1, USB2→Output2 saved to **Hosted slot**.

## Mac mini state changes (persistent)
- Default audio output: ES-9 (will auto-fallback to internal speakers when ES-9 absent).
- New script: `~/code/lisbon-av-install/scripts/set_default_output.swift`.

## Not committed
- `main.cpp` hardware note edit
- `scripts/set_default_output.swift`
- this file

Pablo, when you read this — both edits are sane, you can `git add -A && git commit` whenever.
